#include <stdio.h>
#include <stdlib.h>

int main(){
    int numA, numB;
    printf("Digite um número inteiro:\n");
    scanf("%d", numA);
    printf("Digite outro número inteiro:\n");
    scanf("%d", numB);
    sum = numA + numB;
    dif = numA - numB;
    prod = numA * numB;
    divi = numA / numB;
    return 0;
}